---
const { title = 'say hello, or don’t — the noodle keeps slipping either way.', line = 'slippingtimenoodles.com · built to loop, not to finish' } = Astro.props;
---

<footer>
  <p class="foot-title">{title}</p>
  <p class="foot-line">{line}</p>
</footer>