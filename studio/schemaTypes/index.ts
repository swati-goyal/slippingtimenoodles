import { artwork } from './artwork';
import { archiveEntry } from './archiveEntry';
import { homePage } from './homePage';
import { writing } from './writing';

export const schemaTypes = [writing, artwork, archiveEntry, homePage];
